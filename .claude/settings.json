{
  "permissions": {
    "allow": [
      "Bash(node_modules/.bin/ng build*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -q '^npm run build' && echo '{\"continue\": false, \"stopReason\": \"Use node_modules/.bin/ng build instead of npm run build (npm install fails without network)\"}' || true"
          }
        ]
      }
    ]
  }
}
